
# Local Passive Assistant

This Python application runs on Windows, capturing screen content, monitoring the active window, and extracting text and clipboard data. It logs events locally every 10 seconds.

## How to Run
1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```
2. Run the assistant:
   ```
   python main.py
   ```
    